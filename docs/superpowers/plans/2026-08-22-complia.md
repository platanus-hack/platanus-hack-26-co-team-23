# CumplIA — Implementation Plan (Platanus Hack 2026, 36h)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** App que ingesta normativa colombiana (SUIN-Juriscol), la estructura con Claude, la cruza con el perfil de cada empresa y entrega alertas a Slack (Plus) + abre PRs de cumplimiento con revisor asignado (PRO) + expone todo como MCP server.

**Architecture:** Monolito Next.js 15 (App Router) desplegado en Vercel: API routes hacen de pipeline (ingesta → estructuración → matching → entrega) disparadas por cron/botón; Supabase da Postgres + Auth (Google SSO); Octokit abre los PRs; un endpoint `/api/mcp` expone las normas a agentes externos.

**Tech Stack:** Next.js 15 + TypeScript, Supabase (`@supabase/supabase-js`, `@supabase/ssr`), `@anthropic-ai/sdk` (modelo `claude-sonnet-5`), `zod`, `octokit`, `mcp-handler`, `vitest`, pnpm, Vercel.

## Global Constraints

- Todas las rutas de archivo son **relativas a la raíz del repo del equipo**. El plan asume repo Next.js vacío o recién creado.
- Node ≥ 20, gestor de paquetes **pnpm**.
- Modelo LLM en todo el proyecto: **`claude-sonnet-5`** (constante única en `src/lib/llm.ts` — nunca hardcodear en otro archivo).
- Taxonomía de sectores **cerrada** (ver `SECTORS` en Task 0) — LLM y formularios usan exactamente esa lista.
- Copy de UI en **español**.
- El agente PRO **nunca mergea** un PR — solo lo abre y asigna reviewer.
- Columnas DB y campos TS en `snake_case` (sin capa de mapeo).
- Env vars requeridas (`.env.local`, y en Vercel): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`, `GITHUB_TOKEN`, `CRON_SECRET`, `RESEND_API_KEY`; para los canales wow (Task 5b): `KAPSO_API_KEY`, `KAPSO_PHONE_NUMBER_ID`, `RETELL_API_KEY`, `RETELL_FROM_NUMBER`, `RETELL_AGENT_ID`.
- **Entrega multicanal vía patrón adapter:** cada canal es un archivo en `src/lib/deliver/channels/` que implementa `ChannelAdapter` y se registra con UNA línea en `dispatch.ts`. Agregar un canal nunca toca el pipeline, el matching ni otros canales.
- Commits: Conventional Commits (`feat:`, `fix:`, `chore:`, `test:`).
- **Fuera de alcance del hackathon** (decisión consciente, no olvido): free tier/boletín, Diario Oficial scraping (SUIN + seed curado bastan para la demo), multi-país (el schema ya trae `country`), billing.

## Reparto por miembro (tracks paralelos)

| Miembro | Track | Tareas | Depende de |
|---|---|---|---|
| M1 | Data pipeline (fuentes SUIN + DIAN + SFC + SIC + estructuración) | 1, 1b, 1c, 2 | Task 0 (1c es delegable a quien vaya adelantado) |
| M2 | Auth + Dashboard de configuración | 3, 4 | Task 0 |
| M3 | Matching + dispatcher multicanal + canales base + feed | 5, 6 | Task 0 (usa seed; re-corre con datos reales cuando M1 termine) |
| M4 | PRO: análisis de repo + PR + reviewer | 7 | Task 0 (usa seed) |
| M5 | MCP server + deploy + demo | 8, 9 | Task 0 (usa seed) |
| M2 (tras Task 4) | Canales wow: WhatsApp (Kapso) + llamada de voz (Retell) | 5b | Task 5 (contrato `ChannelAdapter`) |

**Task 0 la hace un solo miembro (M1) con todos mirando** — es el contrato compartido (schema + types + seed). Nadie arranca su track hasta que Task 0 esté mergeada a `main`.

---

### Task 0: Fundación — scaffold, schema, types, seed (M1, bloquea a todos)

**Files:**
- Create: proyecto Next.js completo (scaffold)
- Create: `supabase/schema.sql`
- Create: `supabase/seed.sql`
- Create: `src/lib/types.ts`
- Create: `src/lib/llm.ts`
- Create: `src/lib/supabase/admin.ts`
- Create: `.env.local` (no se commitea), `.env.example`

**Interfaces:**
- Produces: tablas `norms`, `companies`, `alerts`; tipos `Norm`, `Company`, `Alert`, `SECTORS`; cliente `supabaseAdmin()`; constante `MODEL`. **Todos los tracks consumen esto.**

- [x] **Step 1: Scaffold del proyecto**

```bash
pnpm create next-app@latest . --typescript --app --src-dir --no-tailwind --eslint --import-alias "@/*"
pnpm add @supabase/supabase-js @supabase/ssr @anthropic-ai/sdk zod octokit mcp-handler
pnpm add -D vitest
```

- [x] **Step 2: Crear proyecto en supabase.com** (plan free), habilitar **Google provider** en Authentication → Providers (crear OAuth client en Google Cloud Console, redirect URI: `https://<project>.supabase.co/auth/v1/callback`). Copiar URL + keys a `.env.local` y crear `.env.example` con las 6 vars vacías.

- [x] **Step 3: Schema** — pegar en el SQL Editor de Supabase y guardar copia en `supabase/schema.sql`:

```sql
create table norms (
  id uuid primary key default gen_random_uuid(),
  source text not null,                    -- 'suin' | 'seed'
  external_id text unique not null,        -- id de la norma en la fuente
  country text not null default 'CO',
  title text not null,
  issuer text,
  norm_type text,                          -- ley | decreto | resolucion | circular
  published_at date,
  url text,
  raw_text text,
  -- resultado de estructuración LLM (null hasta analizar):
  summary text,
  sectors text[] default '{}',
  company_types text[] default '{}',
  obligations jsonb default '[]',          -- [{action, deadline}]
  severity text,                           -- info | low | medium | high
  analyzed_at timestamptz
);

create table companies (
  id uuid primary key default gen_random_uuid(),
  owner_user_id uuid references auth.users not null,
  name text not null,
  company_type text not null,              -- 'SAS' | 'SA' | 'LTDA' | 'persona natural'
  sectors text[] not null default '{}',
  channels jsonb not null default '[]',    -- [{type, min_severity?, config}] — ver ChannelConfig en types.ts
  github_repo text,                        -- 'owner/repo' (PRO)
  reviewer_github text,                    -- username del Tech Lead revisor (PRO)
  created_at timestamptz default now()
);

create table alerts (
  id uuid primary key default gen_random_uuid(),
  company_id uuid references companies not null,
  norm_id uuid references norms not null,
  impact text not null,
  recommendation text not null,
  pr_url text,                             -- null hasta que PRO abre PR
  created_at timestamptz default now(),
  unique (company_id, norm_id)
);

alter table companies enable row level security;
create policy "own company" on companies for all
  using (auth.uid() = owner_user_id) with check (auth.uid() = owner_user_id);
alter table alerts enable row level security;
create policy "own alerts" on alerts for select
  using (company_id in (select id from companies where owner_user_id = auth.uid()));
alter table norms enable row level security;
create policy "norms are public" on norms for select using (true);
```

- [x] **Step 4: Seed con 3 normas curadas** (para que M3/M4/M5 trabajen sin esperar a M1). Guardar en `supabase/seed.sql` y ejecutar en el SQL Editor. Antes del hackathon, reemplazar `raw_text` por el texto real de 3 normas recientes con impacto claro en código (candidatas: circular de Superfinanciera sobre reporting, resolución DIAN de facturación electrónica, decreto de habeas data):

```sql
insert into norms (source, external_id, title, issuer, norm_type, published_at, url, raw_text,
  summary, sectors, company_types, obligations, severity, analyzed_at) values
('seed', 'seed-001', 'Resolución DIAN — nuevos campos obligatorios en factura electrónica',
 'DIAN', 'resolucion', '2026-08-10', 'https://www.dian.gov.co/', 'TEXTO REAL DE LA NORMA AQUÍ',
 'La DIAN exige dos campos adicionales en el XML de factura electrónica a partir de octubre 2026.',
 array['fintech','comercio','tecnologia'], array['SAS','SA','LTDA'],
 '[{"action":"Agregar campos al XML de facturación","deadline":"2026-10-01"}]', 'high', now()),
('seed', 'seed-002', 'Circular Superfinanciera — retención de logs de transacciones',
 'Superfinanciera', 'circular', '2026-08-12', 'https://www.superfinanciera.gov.co/', 'TEXTO REAL AQUÍ',
 'Entidades vigiladas deben retener logs de transacciones 5 años con integridad verificable.',
 array['fintech'], array['SAS','SA'],
 '[{"action":"Extender retención de logs a 5 años","deadline":"2026-12-01"}]', 'high', now()),
('seed', 'seed-003', 'Decreto — consentimiento explícito para tratamiento de datos biométricos',
 'MinTIC', 'decreto', '2026-08-15', 'https://www.mintic.gov.co/', 'TEXTO REAL AQUÍ',
 'Todo tratamiento de datos biométricos requiere consentimiento explícito y revocable.',
 array['datos-personales','tecnologia','salud','fintech'], array['SAS','SA','LTDA','persona natural'],
 '[{"action":"Agregar flujo de consentimiento biométrico","deadline":null}]', 'medium', now());
```

- [x] **Step 5: Tipos compartidos** — `src/lib/types.ts`:

```typescript
export const SECTORS = [
  'fintech', 'salud', 'alimentos', 'transporte', 'construccion',
  'comercio', 'tecnologia', 'datos-personales', 'laboral-general', 'tributario-general',
] as const
export type Sector = (typeof SECTORS)[number]

export const COMPANY_TYPES = ['SAS', 'SA', 'LTDA', 'persona natural'] as const

export type Obligation = { action: string; deadline: string | null }

export type Norm = {
  id: string; source: string; external_id: string; country: string
  title: string; issuer: string | null; norm_type: string | null
  published_at: string | null; url: string | null; raw_text: string | null
  summary: string | null; sectors: string[]; company_types: string[]
  obligations: Obligation[]; severity: 'info' | 'low' | 'medium' | 'high' | null
  analyzed_at: string | null
}

export const CHANNEL_TYPES = ['slack', 'google_chat', 'discord', 'teams', 'email', 'whatsapp', 'voice'] as const
export type ChannelType = (typeof CHANNEL_TYPES)[number]

export type ChannelConfig = {
  type: ChannelType
  min_severity?: 'low' | 'medium' | 'high'   // omitido = recibe todo
  config: Record<string, string>              // webhook_url | address | phone, según el canal
}

export type Company = {
  id: string; owner_user_id: string; name: string; company_type: string
  sectors: string[]; channels: ChannelConfig[]
  github_repo: string | null; reviewer_github: string | null
}

export type Alert = {
  id: string; company_id: string; norm_id: string
  impact: string; recommendation: string; pr_url: string | null; created_at: string
}
```

- [x] **Step 6: Cliente admin y constante de modelo** — `src/lib/supabase/admin.ts`:

```typescript
import { createClient } from '@supabase/supabase-js'

export function supabaseAdmin() {
  return createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { persistSession: false } },
  )
}
```

`src/lib/llm.ts`:

```typescript
import Anthropic from '@anthropic-ai/sdk'

export const MODEL = 'claude-sonnet-5'
export const anthropic = new Anthropic() // lee ANTHROPIC_API_KEY del env
```

- [x] **Step 7: Configurar vitest** — agregar a `package.json` scripts: `"test": "vitest run"`. Verificar que todo compila:

Run: `pnpm build`
Expected: build exitoso sin errores de tipos.

- [x] **Step 8: Commit y push a main**

```bash
git add -A && git commit -m "feat: scaffold, schema, seed y tipos compartidos (Task 0)" && git push origin main
```

**A partir de aquí todos los miembros hacen `git pull`, crean su rama (`track/m1-pipeline`, etc.) y trabajan en paralelo. Merges a main frecuentes y pequeños.**

---

### Task 1: Ingesta multi-fuente — contrato + adapter SUIN (M1)

**Files:**
- Create: `src/lib/ingest/types.ts` (contrato `SourceAdapter` — espejo del `ChannelAdapter` de salida)
- Create: `src/lib/ingest/ingest.ts` (registro de fuentes + upsert compartido)
- Create: `src/lib/ingest/sources/suin.ts`
- Create: `src/lib/ingest/sources/suin.test.ts`

**Interfaces:**
- Consumes: `supabaseAdmin()` (Task 0)
- Produces: `type SourceNorm = { external_id: string; source: string; title: string; issuer: string | null; norm_type: string | null; published_at: string | null; url: string | null; raw_text: string }`; `interface SourceAdapter { id: string; fetch(limit: number): Promise<SourceNorm[]> }`; `ingestAll(limitPerSource?: number): Promise<Record<string, number>>` que itera el registro `SOURCES`. **Task 1b agrega el adapter DIAN registrándolo con una línea; agregar cualquier fuente futura (Superfinanciera, LeyChile) es el mismo movimiento.**

- [x] **Step 1: Inspeccionar los campos reales del dataset** — VERIFICADO 2026-08-22 con curl real:

Run: `curl -s "https://www.datos.gov.co/resource/fiev-nid6.json?\$limit=2"`
Hallazgos (el código del Step 3 ya usa estos campos):
- Campos reales: `tipo`, `n_mero`, `a_o` (año, como texto), `subtipo`, `sector`, `entidad`, `materia` (pipe-separated), `vigencia`, `art_culos`. Total: 89.366 registros, incluye 2025/2026.
- **NO trae** título largo, URL, fecha exacta ni texto completo → el título se construye (`DECRETO 6 de 2025 — MINISTERIO DE HACIENDA`), `raw_text` se arma con la metadata (`sector`/`materia`/`entidad` — suficiente para clasificar), y el análisis de impacto profundo se demuestra con el seed (texto real curado).
- **SÍ trae `sector` y `materia` oficiales de MinJusticia** — se pasan a Claude como contexto, mejora la clasificación gratis.
- Filtrar por `$where=a_o in ('2025','2026') AND vigencia='Vigente'` (no ordenar por `a_o` globalmente: es texto y hay basura histórica tipo `"996"`).
- Strings con `NULL` literal (texto) — normalizar a null en el mapeo.

- [x] **Step 2: Test del mapeo (falla primero)** — `src/lib/ingest/suin.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { mapSuinRecord } from './suin'

describe('mapSuinRecord', () => {
  it('mapea un registro SODA real a SuinRow', () => {
    // shape real verificado contra el dataset fiev-nid6
    const raw = { tipo: 'DECRETO', n_mero: '6', a_o: '2025', subtipo: 'DECRETO ORDINARIO',
      sector: 'Hacienda y Crédito Público', entidad: 'MINISTERIO DE HACIENDA Y CRÉDITO PÚBLICO',
      materia: 'Tesorería', vigencia: 'Vigente' }
    const row = mapSuinRecord(raw)
    expect(row).toEqual({
      external_id: 'suin-DECRETO-6-2025',
      source: 'suin',
      title: 'DECRETO 6 de 2025 — MINISTERIO DE HACIENDA Y CRÉDITO PÚBLICO',
      issuer: 'MINISTERIO DE HACIENDA Y CRÉDITO PÚBLICO',
      norm_type: 'decreto',
      published_at: '2025-01-01',
      url: null,
      raw_text: 'Tipo: DECRETO (DECRETO ORDINARIO)\nEntidad: MINISTERIO DE HACIENDA Y CRÉDITO PÚBLICO\nSector (MinJusticia): Hacienda y Crédito Público\nMateria: Tesorería\nVigencia: Vigente',
    })
  })
  it('normaliza los "NULL" literales del dataset', () => {
    const row = mapSuinRecord({ tipo: 'RESOLUCION', n_mero: '179', a_o: '2025', subtipo: 'NULL', materia: 'NULL', vigencia: 'NULL' })
    expect(row.raw_text).not.toContain('NULL')
    expect(row.external_id).toBe('suin-RESOLUCION-179-2025')
  })
})
```

Run: `pnpm vitest run src/lib/ingest` → Expected: FAIL (`mapSuinRecord` no existe).

- [x] **Step 3: Contrato de fuente** — `src/lib/ingest/types.ts` (espejo del contrato de canales de salida):

```typescript
export type SourceNorm = {
  external_id: string; source: string; title: string; issuer: string | null
  norm_type: string | null; published_at: string | null; url: string | null
  raw_text: string
}

// Cada fuente de normativa es un archivo que implementa esto y se registra en ingest.ts.
export interface SourceAdapter {
  id: string
  fetch(limit: number): Promise<SourceNorm[]>
}
```

- [x] **Step 4: Adapter SUIN** — `src/lib/ingest/sources/suin.ts` (campos reales verificados en Step 1):

```typescript
import type { SourceAdapter, SourceNorm } from '../types'

// El dataset trae "NULL" como texto literal — normalizar a null real
const clean = (v: unknown): string | null =>
  typeof v === 'string' && v !== 'NULL' && v.trim() ? v : null

export function mapSuinRecord(raw: Record<string, unknown>): SourceNorm {
  const tipo = clean(raw.tipo) ?? 'NORMA'
  const numero = clean(raw.n_mero) ?? '?'
  const anio = clean(raw.a_o) ?? '?'
  const subtipo = clean(raw.subtipo)
  const entidad = clean(raw.entidad)
  const sector = clean(raw.sector)
  const materia = clean(raw.materia)
  const vigencia = clean(raw.vigencia)
  return {
    external_id: `suin-${tipo}-${numero}-${anio}`,
    source: 'suin',
    title: `${tipo} ${numero} de ${anio}${entidad ? ` — ${entidad}` : ''}`,
    issuer: entidad,
    norm_type: tipo.toLowerCase(),
    // el dataset solo trae el año — aproximación para ordenar, no fecha real de publicación
    published_at: /^\d{4}$/.test(anio) ? `${anio}-01-01` : null,
    url: null, // el dataset no trae URL; la ficha vive en suin-juriscol.gov.co (stretch: scrape del detalle)
    // metadata como pseudo-texto: sector/materia oficiales de MinJusticia dan contexto real a Claude
    raw_text: [
      `Tipo: ${tipo}${subtipo ? ` (${subtipo})` : ''}`,
      entidad && `Entidad: ${entidad}`,
      sector && `Sector (MinJusticia): ${sector}`,
      materia && `Materia: ${materia}`,
      vigencia && `Vigencia: ${vigencia}`,
    ].filter(Boolean).join('\n'),
  }
}

export const suin: SourceAdapter = {
  id: 'suin',
  async fetch(limit = 25) {
    // Solo años recientes y vigentes; a_o es texto con basura histórica — jamás ordenar por a_o global
    const where = encodeURIComponent(`a_o in ('2025','2026') AND vigencia='Vigente'`)
    const res = await fetch(
      `https://www.datos.gov.co/resource/fiev-nid6.json?$limit=${limit}&$where=${where}`,
      { headers: { Accept: 'application/json' } },
    )
    if (!res.ok) throw new Error(`SODA ${res.status}`)
    return ((await res.json()) as Record<string, unknown>[]).map(mapSuinRecord)
  },
}
```

- [x] **Step 5: Registro de fuentes + upsert compartido** — `src/lib/ingest/ingest.ts` (una fuente caída no bloquea a las demás — mismo principio que el dispatcher de canales):

```typescript
import { supabaseAdmin } from '@/lib/supabase/admin'
import { suin } from './sources/suin'
import type { SourceAdapter } from './types'

// Task 1b agrega `dian` y Task 1c `superfinanciera`+`sic` — UNA línea cada una. Futuras (leychile) igual.
export const SOURCES: SourceAdapter[] = [suin]

export async function ingestAll(limitPerSource = 25): Promise<Record<string, number>> {
  const db = supabaseAdmin()
  const stats: Record<string, number> = {}
  for (const src of SOURCES) {
    try {
      const rows = await src.fetch(limitPerSource)
      const { error, count } = await db
        .from('norms')
        .upsert(rows, { onConflict: 'external_id', ignoreDuplicates: true, count: 'exact' })
      if (error) throw error
      stats[src.id] = count ?? 0
    } catch (e) {
      console.error(`fuente ${src.id} falló:`, e)
      stats[src.id] = -1 // -1 = fuente caída; las demás siguen
    }
  }
  return stats
}
```

- [x] **Step 6: Verificar** — Run: `pnpm vitest run src/lib/ingest` → Expected: PASS (2 tests).

- [x] **Step 7: Commit**

```bash
git add src/lib/ingest && git commit -m "feat: contrato SourceAdapter e ingesta SUIN via SODA API"
```

---

### Task 1b: Adapter DIAN — Normograma con texto completo (M1)

**Files:**
- Create: `src/lib/ingest/sources/dian.ts`
- Create: `src/lib/ingest/sources/dian.test.ts`
- Modify: `src/lib/ingest/ingest.ts` (1 línea: agregar `dian` a `SOURCES`)

**Interfaces:**
- Consumes: `SourceAdapter`, `SourceNorm` (Task 1)
- Produces: adapter `dian` registrado. **La joya: el Normograma DIAN publica el TEXTO COMPLETO de cada resolución en HTML** (`https://normograma.dian.gov.co/dian/compilacion/docs/resolucion_dian_0227_2025.htm`) — a diferencia de SUIN que solo da metadata. Las normas DIAN llegan con `raw_text` real → el análisis de impacto y el PR del tier PRO funcionan con normativa tributaria real, sin depender del seed.

- [x] **Step 1: Test del parseo de links (falla primero)** — `src/lib/ingest/sources/dian.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { parseDianLink, stripHtml } from './dian'

describe('parseDianLink', () => {
  it('extrae tipo, número y año del nombre de archivo del Normograma', () => {
    expect(parseDianLink('docs/resolucion_dian_0227_2025.htm')).toEqual({
      external_id: 'dian-resolucion_dian_0227_2025',
      norm_type: 'resolucion',
      year: '2025',
      url: 'https://normograma.dian.gov.co/dian/compilacion/docs/resolucion_dian_0227_2025.htm',
    })
  })
})

describe('stripHtml', () => {
  it('deja solo el texto', () => {
    expect(stripHtml('<p>Art&nbsp;1. <b>Plazo</b>:</p><script>x()</script> 2026')).toBe('Art 1. Plazo : 2026')
  })
})
```

Run: `pnpm vitest run src/lib/ingest/sources/dian.test.ts` → Expected: FAIL.

- [x] **Step 2: Implementación** — `src/lib/ingest/sources/dian.ts`:

```typescript
import type { SourceAdapter, SourceNorm } from '../types'

const BASE = 'https://normograma.dian.gov.co/dian/compilacion'

export function stripHtml(html: string): string {
  return html
    .replace(/<script[\s\S]*?<\/script>/gi, ' ')
    .replace(/<style[\s\S]*?<\/style>/gi, ' ')
    .replace(/<[^>]+>/g, ' ')
    .replace(/&nbsp;/g, ' ')
    .replace(/\s+/g, ' ')
    .trim()
}

export function parseDianLink(link: string) {
  const file = link.split('/')[1].replace('.htm', '') // resolucion_dian_0227_2025
  return {
    external_id: `dian-${file}`,
    norm_type: file.split('_')[0],
    year: file.split('_').at(-1)!,
    url: `${BASE}/${link}`,
  }
}

export const dian: SourceAdapter = {
  id: 'dian',
  async fetch(limit = 10) {
    const index = await (await fetch(`${BASE}/normatividad_institucional_dian.html`)).text()
    // links tipo docs/resolucion_dian_0227_2025.htm — solo años recientes, dedupe
    const links = [...new Set(index.match(/docs\/[a-z]+_dian_\d+_202[5-9]\.htm/g) ?? [])].slice(0, limit)
    const norms: SourceNorm[] = []
    for (const link of links) {
      const meta = parseDianLink(link)
      const html = await (await fetch(meta.url)).text()
      const title = stripHtml(html.match(/<title>([\s\S]*?)<\/title>/i)?.[1] ?? meta.external_id)
      norms.push({
        ...meta,
        source: 'dian',
        title,
        issuer: 'DIAN',
        published_at: `${meta.year}-01-01`,
        raw_text: stripHtml(html).slice(0, 60000), // texto COMPLETO — ventaja de esta fuente
      })
    }
    return norms
  },
}
```

Y en `src/lib/ingest/ingest.ts`: importar y agregar `dian` a `SOURCES`.

- [x] **Step 3: Verificar** — Run: `pnpm vitest run src/lib/ingest` → PASS. Luego smoke real (el HTML del Normograma puede variar — si el regex del índice no matchea nada, abrir la página en el browser y ajustar el patrón):

Run: `node -e "import('./src/lib/ingest/sources/dian.ts')"` no aplica en TS — probar vía el endpoint en Task 2 (`curl` al ingest) y revisar en Supabase que aparezcan filas `source='dian'` con `raw_text` largo.

- [x] **Step 4: Commit**

```bash
git add src/lib/ingest && git commit -m "feat: adapter DIAN via Normograma con texto completo"
```

---

### Task 1c: Adapters Superfinanciera + SIC (M1, o quien vaya adelantado)

**Files:**
- Create: `src/lib/ingest/scrape.ts` (mover `stripHtml` aquí desde `dian.ts` — helper compartido de scraping)
- Modify: `src/lib/ingest/sources/dian.ts` (importar `stripHtml` de `../scrape`)
- Create: `src/lib/ingest/sources/superfinanciera.ts`
- Create: `src/lib/ingest/sources/superfinanciera.test.ts`
- Create: `src/lib/ingest/sources/sic.ts`
- Modify: `src/lib/ingest/ingest.ts` (2 líneas: registrar ambos en `SOURCES`)

**Interfaces:**
- Consumes: `SourceAdapter`, `SourceNorm` (Task 1), `stripHtml`
- Produces: adapters `superfinanciera` y `sic` registrados. Verificado contra los sitios vivos (2026-08-22):
  - **SFC**: páginas anuales server-rendered con URL fija — `/publicaciones/10115974/circulares-externas-2026/` y `/publicaciones/10115459/circulares-externas-2025/` (HTTP 200). Los documentos son PDFs tras `loader.php?...idFile=N` → para el hackathon el `raw_text` es el texto del listado (número + asunto de cada circular), suficiente para clasificar y alertar; texto completo del PDF = stretch post-hackathon (`pdf-parse`).
  - **SIC**: repositorio Drupal server-rendered filtrable — `/repositorio-de-normatividad?field_tipo_de_norma_value=3` (resoluciones) y `=5` (circulares). Las fichas son nodos HTML → texto completo disponible con el mismo `stripHtml`.
- ⚠️ Los regex de ambos índices se validan contra el HTML vivo en el Step 4 (mismo protocolo que DIAN); si un sitio cambia o bloquea, `ingestAll` aísla la fuente (-1) y las demás siguen.

- [x] **Step 1: Test del parser SFC (falla primero)** — `src/lib/ingest/sources/superfinanciera.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { parseSfcListing } from './superfinanciera'

describe('parseSfcListing', () => {
  it('extrae circulares del texto del listado anual', () => {
    const text = 'Circular Externa 004 de 2026 Adopción de la nueva Circular Básica Financiera ' +
      'Circular Externa 002 de 2026 Modifica los plazos de implementación del MURIC'
    const items = parseSfcListing(text, '2026')
    expect(items).toHaveLength(2)
    expect(items[0]).toMatchObject({
      external_id: 'sfc-circular-externa-004-2026',
      title: expect.stringContaining('Circular Externa 004 de 2026'),
      norm_type: 'circular',
      issuer: 'Superfinanciera',
    })
    expect(items[0].raw_text).toContain('Circular Básica Financiera')
  })
})
```

Run: `pnpm vitest run src/lib/ingest/sources/superfinanciera.test.ts` → Expected: FAIL.

- [x] **Step 2: Adapter Superfinanciera** — `src/lib/ingest/sources/superfinanciera.ts`:

```typescript
import type { SourceAdapter, SourceNorm } from '../types'
import { stripHtml } from '../scrape'

// Páginas anuales de circulares externas (server-rendered, verificadas). Agregar año nuevo = 1 línea.
const YEAR_PAGES: Record<string, string> = {
  '2026': 'https://www.superfinanciera.gov.co/publicaciones/10115974/circulares-externas-2026/',
  '2025': 'https://www.superfinanciera.gov.co/publicaciones/10115459/circulares-externas-2025/',
}

// Del texto plano del listado: cada "Circular Externa NNN de YYYY" + su asunto (hasta la siguiente circular)
export function parseSfcListing(text: string, year: string): SourceNorm[] {
  const url = YEAR_PAGES[year]
  const matches = [...text.matchAll(/Circular Externa (\d{1,3}) de (\d{4})/g)]
  return matches
    .filter((m) => m[2] === year)
    .map((m, i) => {
      const start = m.index!
      const end = matches[i + 1]?.index ?? Math.min(start + 400, text.length)
      const chunk = text.slice(start, end).trim()
      return {
        external_id: `sfc-circular-externa-${m[1].padStart(3, '0')}-${year}`,
        source: 'superfinanciera',
        title: chunk.slice(0, 120),
        issuer: 'Superfinanciera',
        norm_type: 'circular',
        published_at: `${year}-01-01`,
        url: url ?? null,
        raw_text: chunk, // número + asunto del listado; texto completo del PDF = stretch
      }
    })
}

export const superfinanciera: SourceAdapter = {
  id: 'superfinanciera',
  async fetch(limit = 20) {
    const norms: SourceNorm[] = []
    for (const [year, url] of Object.entries(YEAR_PAGES)) {
      const html = await (await fetch(url, { headers: { 'User-Agent': 'Mozilla/5.0' } })).text()
      norms.push(...parseSfcListing(stripHtml(html), year))
    }
    return norms.slice(0, limit)
  },
}
```

Run: `pnpm vitest run src/lib/ingest/sources/superfinanciera.test.ts` → Expected: PASS.

- [x] **Step 3: Adapter SIC** — `src/lib/ingest/sources/sic.ts` (las fichas son nodos HTML → texto completo):

```typescript
import type { SourceAdapter, SourceNorm } from '../types'
import { stripHtml } from '../scrape'

const BASE = 'https://www.sic.gov.co'
// tipo 3 = resoluciones, tipo 5 = circulares (verificado en el repositorio Drupal)
const LISTINGS = [
  { url: `${BASE}/repositorio-de-normatividad?field_tipo_de_norma_value=3`, norm_type: 'resolucion' },
  { url: `${BASE}/repositorio-de-normatividad?field_tipo_de_norma_value=5`, norm_type: 'circular' },
]

export const sic: SourceAdapter = {
  id: 'sic',
  async fetch(limit = 10) {
    const norms: SourceNorm[] = []
    for (const listing of LISTINGS) {
      const html = await (await fetch(listing.url, { headers: { 'User-Agent': 'Mozilla/5.0' } })).text()
      // filas del view Drupal: anchors a la ficha con título largo (excluye menús, que son cortos o nav-link)
      const rows = [...html.matchAll(/<a href="(\/[^"?]+)"[^>]*>([^<]{25,140})<\/a>/g)]
        .filter(([full]) => !full.includes('nav-link'))
        .slice(0, Math.ceil(limit / LISTINGS.length))
      for (const [, path, title] of rows) {
        const ficha = await (await fetch(`${BASE}${path}`, { headers: { 'User-Agent': 'Mozilla/5.0' } })).text()
        norms.push({
          external_id: `sic-${path.split('/').filter(Boolean).join('-')}`,
          source: 'sic',
          title: title.trim(),
          issuer: 'SIC',
          norm_type: listing.norm_type,
          published_at: title.match(/de (20\d\d)/)?.[1].concat('-01-01') ?? null,
          url: `${BASE}${path}`,
          raw_text: stripHtml(ficha).slice(0, 60000), // ficha HTML = texto completo
        })
      }
    }
    return norms
  },
}
```

- [x] **Step 4: Registrar y validar contra los sitios vivos** — en `ingest.ts`: `SOURCES = [suin, dian, superfinanciera, sic]`. Luego, con `pnpm dev` corriendo:

Run: `curl -s -X POST -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/ingest`
Expected: `{"inserted":{"suin":N,"dian":M,"superfinanciera":K,"sic":J},...}` con K≥1 y J≥1. Revisar en Supabase 2-3 filas de cada fuente: título coherente, sin restos de HTML/menús. Si una fuente trae basura, ajustar su regex contra el HTML vivo (`curl <url> -A "Mozilla/5.0" | less`) — el contrato no cambia.

- [x] **Step 5: Commit**

```bash
git add src/lib/ingest && git commit -m "feat: adapters Superfinanciera y SIC"
```

---

### Task 2: Estructuración con Claude + endpoint de pipeline (M1)

**Files:**
- Create: `src/lib/ingest/analyze.ts`
- Create: `src/lib/ingest/analyze.test.ts`
- Create: `src/app/api/cron/ingest/route.ts`

**Interfaces:**
- Consumes: `MODEL`, `anthropic` (Task 0); `fetchSuinNorms`, `upsertNorms` (Task 1); tipos `SECTORS`, `Obligation`
- Produces: `analyzeNorm(title: string, rawText: string): Promise<NormAnalysis>` con `NormAnalysis = { summary: string; sectors: Sector[]; company_types: string[]; obligations: Obligation[]; severity: 'info'|'low'|'medium'|'high' }`; endpoint `POST /api/cron/ingest` (header `Authorization: Bearer $CRON_SECRET`)

- [x] **Step 1: Test del schema de salida (falla primero)** — `src/lib/ingest/analyze.test.ts` (testea el parseo/validación, no la llamada al LLM):

```typescript
import { describe, it, expect } from 'vitest'
import { parseAnalysis } from './analyze'

describe('parseAnalysis', () => {
  it('extrae y valida el JSON de la respuesta del modelo', () => {
    const llmText = 'Aquí está el análisis:\n{"summary":"Nueva obligación de reporte.","sectors":["fintech"],"company_types":["SAS"],"obligations":[{"action":"Enviar reporte mensual","deadline":"2026-10-01"}],"severity":"high"}'
    const a = parseAnalysis(llmText)
    expect(a.severity).toBe('high')
    expect(a.sectors).toEqual(['fintech'])
  })
  it('rechaza sectores fuera de la taxonomía', () => {
    expect(() => parseAnalysis('{"summary":"x","sectors":["criptogamer"],"company_types":[],"obligations":[],"severity":"low"}')).toThrow()
  })
})
```

Run: `pnpm vitest run src/lib/ingest/analyze.test.ts` → Expected: FAIL.

- [x] **Step 2: Implementación** — `src/lib/ingest/analyze.ts`:

```typescript
import { z } from 'zod'
import { anthropic, MODEL } from '@/lib/llm'
import { SECTORS } from '@/lib/types'

const AnalysisSchema = z.object({
  summary: z.string().min(10),
  sectors: z.array(z.enum(SECTORS)),
  company_types: z.array(z.string()),
  obligations: z.array(z.object({ action: z.string(), deadline: z.string().nullable() })),
  severity: z.enum(['info', 'low', 'medium', 'high']),
})
export type NormAnalysis = z.infer<typeof AnalysisSchema>

export function parseAnalysis(text: string): NormAnalysis {
  const start = text.indexOf('{')
  const end = text.lastIndexOf('}')
  if (start === -1 || end === -1) throw new Error('sin JSON en la respuesta')
  return AnalysisSchema.parse(JSON.parse(text.slice(start, end + 1)))
}

const SYSTEM = `Eres un analista regulatorio colombiano. Dada una norma, responde SOLO un JSON:
{"summary": "resumen en 1-2 frases de qué cambia y a quién obliga",
 "sectors": [...], // SOLO de: ${SECTORS.join(', ')}
 "company_types": [...], // de: SAS, SA, LTDA, persona natural
 "obligations": [{"action": "obligación concreta", "deadline": "YYYY-MM-DD o null"}],
 "severity": "info|low|medium|high"} // high = crea obligaciones con sanción
Sé conservador: si la norma no afecta empresas, sectors=[] y severity=info.`

export async function analyzeNorm(title: string, rawText: string): Promise<NormAnalysis> {
  const msg = await anthropic.messages.create({
    model: MODEL,
    max_tokens: 1200,
    system: SYSTEM,
    messages: [{ role: 'user', content: `TÍTULO: ${title}\n\nTEXTO:\n${rawText.slice(0, 30000)}` }],
  })
  const text = msg.content[0].type === 'text' ? msg.content[0].text : ''
  return parseAnalysis(text)
}
```

- [x] **Step 3: Verificar tests** — Run: `pnpm vitest run src/lib/ingest/analyze.test.ts` → Expected: PASS (2 tests).

- [x] **Step 4: Endpoint de pipeline** — `src/app/api/cron/ingest/route.ts` (ingesta + analiza lo no-analizado; usa `title` como texto cuando la fuente no trae texto completo):

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { supabaseAdmin } from '@/lib/supabase/admin'
import { ingestAll } from '@/lib/ingest/ingest'
import { analyzeNorm } from '@/lib/ingest/analyze'

export const maxDuration = 300

export async function POST(req: NextRequest) {
  if (req.headers.get('authorization') !== `Bearer ${process.env.CRON_SECRET}`)
    return NextResponse.json({ error: 'unauthorized' }, { status: 401 })

  const inserted = await ingestAll(15) // {suin: N, dian: M} — -1 marca fuente caída
  const db = supabaseAdmin()
  const { data: pending } = await db.from('norms').select('*').is('analyzed_at', null).limit(10)

  let analyzed = 0
  for (const norm of pending ?? []) {
    try {
      const a = await analyzeNorm(norm.title, norm.raw_text ?? norm.title)
      await db.from('norms').update({ ...a, analyzed_at: new Date().toISOString() }).eq('id', norm.id)
      analyzed++
    } catch (e) {
      console.error(`analyze falló para ${norm.external_id}:`, e)
    }
  }
  return NextResponse.json({ inserted, analyzed })
}
```

- [x] **Step 5: Verificar end-to-end en local**

Run: `pnpm dev` y en otra terminal: `curl -s -X POST -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/ingest`
Expected: `{"inserted":{"suin":N,"dian":M},"analyzed":K}` con K>0, y en Supabase Table Editor filas de ambas fuentes — las de `source='dian'` con `raw_text` largo (texto completo) y `summary`/`sectors` poblados.

- [x] **Step 6: Commit**

```bash
git add src/lib/ingest src/app/api/cron/ingest && git commit -m "feat: estructuración LLM y endpoint de pipeline de ingesta"
```

---

### Task 3: Auth Google SSO + shell del dashboard (M2)

**Files:**
- Create: `src/lib/supabase/server.ts`, `src/lib/supabase/browser.ts`
- Create: `src/middleware.ts`
- Create: `src/app/login/page.tsx`
- Create: `src/app/auth/callback/route.ts`
- Create: `src/app/(dashboard)/layout.tsx`

**Interfaces:**
- Consumes: proyecto Supabase con Google provider (Task 0 Step 2)
- Produces: `createServerSupabase()` (server components/actions, respeta RLS del usuario) y rutas protegidas bajo `src/app/(dashboard)/`. **M3 y M4 la consumen para leer `companies` del usuario.**

- [ ] **Step 1: Clientes SSR** — `src/lib/supabase/browser.ts`:

```typescript
import { createBrowserClient } from '@supabase/ssr'
export const supabaseBrowser = () =>
  createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!)
```

`src/lib/supabase/server.ts`:

```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createServerSupabase() {
  const store = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { cookies: { getAll: () => store.getAll(), setAll: (all) => all.forEach(({ name, value, options }) => store.set(name, value, options)) } },
  )
}
```

- [ ] **Step 2: Middleware de protección** — `src/middleware.ts`:

```typescript
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function middleware(req: NextRequest) {
  const res = NextResponse.next()
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { cookies: { getAll: () => req.cookies.getAll(), setAll: (all) => all.forEach(({ name, value }) => res.cookies.set(name, value)) } },
  )
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.redirect(new URL('/login', req.url))
  return res
}
export const config = { matcher: ['/settings/:path*', '/feed/:path*'] }
```

- [ ] **Step 3: Login + callback** — `src/app/login/page.tsx`:

```tsx
'use client'
import { supabaseBrowser } from '@/lib/supabase/browser'

export default function Login() {
  const signIn = () =>
    supabaseBrowser().auth.signInWithOAuth({
      provider: 'google',
      options: { redirectTo: `${location.origin}/auth/callback` },
    })
  return (
    <main style={{ display: 'grid', placeItems: 'center', minHeight: '100vh' }}>
      <div style={{ textAlign: 'center' }}>
        <h1>CumplIA</h1>
        <p>El Diario Oficial, vigilado por un agente.</p>
        <button onClick={signIn}>Entrar con Google</button>
      </div>
    </main>
  )
}
```

`src/app/auth/callback/route.ts`:

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { createServerSupabase } from '@/lib/supabase/server'

export async function GET(req: NextRequest) {
  const code = new URL(req.url).searchParams.get('code')
  if (code) {
    const supabase = await createServerSupabase()
    await supabase.auth.exchangeCodeForSession(code)
  }
  return NextResponse.redirect(new URL('/settings', req.url))
}
```

- [ ] **Step 4: Layout del dashboard** — `src/app/(dashboard)/layout.tsx`:

```tsx
import Link from 'next/link'

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return (
    <div style={{ maxWidth: 960, margin: '0 auto', padding: 24 }}>
      <nav style={{ display: 'flex', gap: 16, marginBottom: 24 }}>
        <strong>CumplIA</strong>
        <Link href="/feed">Alertas</Link>
        <Link href="/settings">Configuración</Link>
      </nav>
      {children}
    </div>
  )
}
```

- [ ] **Step 5: Verificar** — Run: `pnpm dev`, abrir `http://localhost:3000/settings`.
Expected: redirige a `/login`; tras "Entrar con Google" vuelve a `/settings` (dará 404 hasta Task 4 — correcto).

- [ ] **Step 6: Commit**

```bash
git add src/lib/supabase src/middleware.ts src/app/login src/app/auth "src/app/(dashboard)" && git commit -m "feat: Google SSO con Supabase y shell del dashboard"
```

---

### Task 4: Dashboard de configuración (M2)

**Files:**
- Create: `src/app/(dashboard)/settings/page.tsx`
- Create: `src/app/(dashboard)/settings/actions.ts`

**Interfaces:**
- Consumes: `createServerSupabase()` (Task 3), `SECTORS`, `COMPANY_TYPES`, `ChannelConfig` (Task 0)
- Produces: fila en `companies` con perfil + `channels: ChannelConfig[]` + config PRO. **M3 lee `channels`, M4 lee `github_repo`/`reviewer_github`.**

- [ ] **Step 1: Server action** — `src/app/(dashboard)/settings/actions.ts`:

```typescript
'use server'
import { createServerSupabase } from '@/lib/supabase/server'
import { revalidatePath } from 'next/cache'

export async function saveCompany(formData: FormData) {
  const supabase = await createServerSupabase()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) throw new Error('no auth')

  // Cada campo ch_* no vacío se vuelve un ChannelConfig; la voz solo suena para normas high.
  const CHANNEL_FIELDS = [
    { type: 'slack', key: 'webhook_url', field: 'ch_slack' },
    { type: 'google_chat', key: 'webhook_url', field: 'ch_gchat' },
    { type: 'discord', key: 'webhook_url', field: 'ch_discord' },
    { type: 'teams', key: 'webhook_url', field: 'ch_teams' },
    { type: 'email', key: 'address', field: 'ch_email' },
    { type: 'whatsapp', key: 'phone', field: 'ch_whatsapp' },
    { type: 'voice', key: 'phone', field: 'ch_voice' },
  ] as const
  const channels = CHANNEL_FIELDS
    .map(({ type, key, field }) => ({ type, config: { [key]: String(formData.get(field) ?? '').trim() } }))
    .filter((c) => Object.values(c.config)[0])
    .map((c) => (c.type === 'voice' ? { ...c, min_severity: 'high' as const } : c))

  const row = {
    owner_user_id: user.id,
    name: String(formData.get('name')),
    company_type: String(formData.get('company_type')),
    sectors: formData.getAll('sectors').map(String),
    channels,
    github_repo: String(formData.get('github_repo')) || null,
    reviewer_github: String(formData.get('reviewer_github')) || null,
  }
  const { data: existing } = await supabase.from('companies').select('id').eq('owner_user_id', user.id).maybeSingle()
  const { error } = existing
    ? await supabase.from('companies').update(row).eq('id', existing.id)
    : await supabase.from('companies').insert(row)
  if (error) throw error
  revalidatePath('/settings')
}
```

- [ ] **Step 2: Página de settings** — `src/app/(dashboard)/settings/page.tsx`:

```tsx
import { createServerSupabase } from '@/lib/supabase/server'
import { SECTORS, COMPANY_TYPES, type ChannelConfig } from '@/lib/types'
import { saveCompany } from './actions'

export default async function Settings() {
  const supabase = await createServerSupabase()
  const { data: { user } } = await supabase.auth.getUser()
  const { data: company } = await supabase.from('companies').select('*').eq('owner_user_id', user!.id).maybeSingle()
  const ch = (t: string) => (company?.channels as ChannelConfig[] | null)?.find((c) => c.type === t)?.config ?? {}

  return (
    <form action={saveCompany} style={{ display: 'grid', gap: 12, maxWidth: 520 }}>
      <h2>Perfil de la empresa</h2>
      <label>Nombre <input name="name" defaultValue={company?.name ?? ''} required /></label>
      <label>Tipo de sociedad{' '}
        <select name="company_type" defaultValue={company?.company_type ?? 'SAS'}>
          {COMPANY_TYPES.map((t) => <option key={t}>{t}</option>)}
        </select>
      </label>
      <fieldset>
        <legend>Sectores que te afectan</legend>
        {SECTORS.map((s) => (
          <label key={s} style={{ display: 'block' }}>
            <input type="checkbox" name="sectors" value={s} defaultChecked={company?.sectors?.includes(s)} /> {s}
          </label>
        ))}
      </fieldset>
      <h2>Canales de alerta (Plus) — llena solo los que uses</h2>
      <label>Slack (webhook) <input name="ch_slack" defaultValue={ch('slack').webhook_url ?? ''} placeholder="https://hooks.slack.com/services/..." /></label>
      <label>Google Chat (webhook) <input name="ch_gchat" defaultValue={ch('google_chat').webhook_url ?? ''} /></label>
      <label>Discord (webhook) <input name="ch_discord" defaultValue={ch('discord').webhook_url ?? ''} /></label>
      <label>Microsoft Teams (webhook) <input name="ch_teams" defaultValue={ch('teams').webhook_url ?? ''} /></label>
      <label>Email <input name="ch_email" type="email" defaultValue={ch('email').address ?? ''} /></label>
      <label>WhatsApp <input name="ch_whatsapp" defaultValue={ch('whatsapp').phone ?? ''} placeholder="+573001234567" /></label>
      <label>Llamada de voz — solo normas críticas <input name="ch_voice" defaultValue={ch('voice').phone ?? ''} placeholder="+573001234567" /></label>
      <h2>Cumplimiento en código (PRO)</h2>
      <label>Repo de GitHub <input name="github_repo" defaultValue={company?.github_repo ?? ''} placeholder="mi-org/mi-repo" /></label>
      <label>Revisor responsable (username GitHub) <input name="reviewer_github" defaultValue={company?.reviewer_github ?? ''} placeholder="tech-lead" /></label>
      <button type="submit">Guardar</button>
    </form>
  )
}
```

- [ ] **Step 3: Verificar** — Run: `pnpm dev`, login, llenar el formulario, Guardar.
Expected: fila en `companies` (Supabase Table Editor) con `owner_user_id` del usuario; recargar muestra los valores persistidos.

- [ ] **Step 4: Commit**

```bash
git add "src/app/(dashboard)/settings" && git commit -m "feat: dashboard de configuración de empresa, canal y PRO"
```

---

### Task 5: Matching + dispatcher multicanal + canales base (M3)

**Files:**
- Create: `src/lib/match.ts`
- Create: `src/lib/match.test.ts`
- Create: `src/lib/deliver/impact.ts`
- Create: `src/lib/deliver/types.ts`
- Create: `src/lib/deliver/format.ts`
- Create: `src/lib/deliver/dispatch.ts`
- Create: `src/lib/deliver/dispatch.test.ts`
- Create: `src/lib/deliver/channels/slack.ts`, `src/lib/deliver/channels/google-chat.ts`, `src/lib/deliver/channels/discord.ts`, `src/lib/deliver/channels/teams.ts`, `src/lib/deliver/channels/email.ts`
- Create: `src/app/api/cron/match/route.ts`

**Interfaces:**
- Consumes: `supabaseAdmin()`, `anthropic`, `MODEL`, tipos `Norm`, `Company`, `ChannelConfig` (Task 0); normas del seed
- Produces: `matches(company: Pick<Company,'sectors'|'company_type'>, norm: Pick<Norm,'sectors'|'company_types'|'severity'>): boolean`; `generateImpact(company: Company, norm: Norm): Promise<{ impact: string; recommendation: string }>`; **el contrato de canal** `AlertPayload` + `interface ChannelAdapter { send(config: Record<string,string>, payload: AlertPayload): Promise<void> }`; `deliverAlert(channels: ChannelConfig[], payload: AlertPayload, registry?): Promise<{ delivered: string[]; failed: string[] }>`; endpoint `POST /api/cron/match` (mismo `CRON_SECRET`). **M4 consume las filas de `alerts`; Task 5b implementa `ChannelAdapter` para whatsapp/voice y los registra en `ADAPTERS`.**

- [ ] **Step 1: Test de matching (falla primero)** — `src/lib/match.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { matches } from './match'

const fintechSAS = { sectors: ['fintech'], company_type: 'SAS' }

describe('matches', () => {
  it('matchea cuando hay intersección de sector y tipo', () => {
    expect(matches(fintechSAS, { sectors: ['fintech', 'comercio'], company_types: ['SAS'], severity: 'high' })).toBe(true)
  })
  it('no matchea sin intersección de sector', () => {
    expect(matches(fintechSAS, { sectors: ['salud'], company_types: ['SAS'], severity: 'high' })).toBe(false)
  })
  it('matchea por sector aunque company_types venga vacío (aplica a todas)', () => {
    expect(matches(fintechSAS, { sectors: ['fintech'], company_types: [], severity: 'medium' })).toBe(true)
  })
  it('ignora normas severity=info (ruido)', () => {
    expect(matches(fintechSAS, { sectors: ['fintech'], company_types: ['SAS'], severity: 'info' })).toBe(false)
  })
})
```

Run: `pnpm vitest run src/lib/match.test.ts` → Expected: FAIL.

- [ ] **Step 2: Implementar matching** — `src/lib/match.ts`:

```typescript
type CompanyProfile = { sectors: string[]; company_type: string }
type NormTags = { sectors: string[]; company_types: string[]; severity: string | null }

export function matches(company: CompanyProfile, norm: NormTags): boolean {
  if (!norm.severity || norm.severity === 'info') return false
  const sectorHit = norm.sectors.some((s) => company.sectors.includes(s))
  const typeHit = norm.company_types.length === 0 || norm.company_types.includes(company.company_type)
  return sectorHit && typeHit
}
```

Run: `pnpm vitest run src/lib/match.test.ts` → Expected: PASS (4 tests).

- [ ] **Step 3: Impacto personalizado** — `src/lib/deliver/impact.ts`:

```typescript
import { anthropic, MODEL } from '@/lib/llm'
import type { Company, Norm } from '@/lib/types'

export async function generateImpact(company: Company, norm: Norm): Promise<{ impact: string; recommendation: string }> {
  const msg = await anthropic.messages.create({
    model: MODEL,
    max_tokens: 500,
    system: 'Eres asesor de cumplimiento. Responde SOLO JSON: {"impact":"cómo afecta esta norma a ESTA empresa, 1-2 frases","recommendation":"acción concreta de mitigación/cumplimiento, 1-2 frases"}. Español, directo, sin jerga legal.',
    messages: [{ role: 'user', content:
      `EMPRESA: ${company.name} (${company.company_type}), sectores: ${company.sectors.join(', ')}\n` +
      `NORMA: ${norm.title}\nRESUMEN: ${norm.summary}\nOBLIGACIONES: ${JSON.stringify(norm.obligations)}` }],
  })
  const text = msg.content[0].type === 'text' ? msg.content[0].text : '{}'
  const json = JSON.parse(text.slice(text.indexOf('{'), text.lastIndexOf('}') + 1))
  return { impact: String(json.impact), recommendation: String(json.recommendation) }
}
```

- [ ] **Step 4: Contrato de canal + formato compartido** — `src/lib/deliver/types.ts`:

```typescript
import type { ChannelConfig } from '@/lib/types'

export type AlertPayload = {
  norm_title: string
  norm_url: string | null
  impact: string
  recommendation: string
  severity: 'low' | 'medium' | 'high'
}

// Contrato único: cada canal es un archivo que implementa esto y se registra en dispatch.ts.
export interface ChannelAdapter {
  send(config: Record<string, string>, payload: AlertPayload): Promise<void>
}

export type { ChannelConfig }
```

`src/lib/deliver/format.ts` (texto base que los canales de texto comparten):

```typescript
import type { AlertPayload } from './types'

const EMOJI: Record<string, string> = { high: '🔴', medium: '🟠', low: '🟡' }

export function formatAlertText(p: AlertPayload): string {
  return [
    `${EMOJI[p.severity]} *Cambio normativo que te afecta*`,
    `*${p.norm_title}*${p.norm_url ? `\n${p.norm_url}` : ''}`,
    `*Cómo te afecta:* ${p.impact}`,
    `*Qué hacer:* ${p.recommendation}`,
    '_CumplIA · esto no constituye asesoría jurídica_',
  ].join('\n\n')
}
```

- [ ] **Step 5: Test del dispatcher (falla primero)** — `src/lib/deliver/dispatch.test.ts` (lógica pura: aislamiento de errores + filtro `min_severity`, con registry inyectado — cero red):

```typescript
import { describe, it, expect } from 'vitest'
import { deliverAlert } from './dispatch'
import type { AlertPayload, ChannelAdapter } from './types'

const payload: AlertPayload = { norm_title: 'X', norm_url: null, impact: 'i', recommendation: 'r', severity: 'medium' }

describe('deliverAlert', () => {
  it('entrega a todos los canales elegibles y aísla errores (un canal caído no bloquea a los demás)', async () => {
    const calls: string[] = []
    const registry: Record<string, ChannelAdapter> = {
      ok: { send: async () => { calls.push('ok') } },
      boom: { send: async () => { throw new Error('down') } },
    }
    const res = await deliverAlert(
      [{ type: 'ok', config: {} }, { type: 'boom', config: {} }] as never, payload, registry,
    )
    expect(calls).toEqual(['ok'])
    expect(res.delivered).toEqual(['ok'])
    expect(res.failed).toEqual(['boom'])
  })

  it('respeta min_severity (la voz no suena por una norma medium)', async () => {
    const calls: string[] = []
    const registry: Record<string, ChannelAdapter> = { voice: { send: async () => { calls.push('voice') } } }
    await deliverAlert([{ type: 'voice', min_severity: 'high', config: {} }] as never, payload, registry)
    expect(calls).toEqual([])
  })

  it('ignora canales sin adapter registrado', async () => {
    const res = await deliverAlert([{ type: 'fax', config: {} }] as never, payload, {})
    expect(res).toEqual({ delivered: [], failed: [] })
  })
})
```

Run: `pnpm vitest run src/lib/deliver` → Expected: FAIL (`dispatch.ts` no existe).

- [ ] **Step 6: Dispatcher** — `src/lib/deliver/dispatch.ts`:

```typescript
import type { AlertPayload, ChannelAdapter, ChannelConfig } from './types'
import { slack } from './channels/slack'
import { googleChat } from './channels/google-chat'
import { discord } from './channels/discord'
import { teams } from './channels/teams'
import { email } from './channels/email'

// Registro de canales. Task 5b agrega whatsapp y voice — UNA línea cada uno.
export const ADAPTERS: Record<string, ChannelAdapter> = {
  slack,
  google_chat: googleChat,
  discord,
  teams,
  email,
}

const RANK = { low: 0, medium: 1, high: 2 } as const

export async function deliverAlert(
  channels: ChannelConfig[],
  payload: AlertPayload,
  registry: Record<string, ChannelAdapter> = ADAPTERS,
): Promise<{ delivered: string[]; failed: string[] }> {
  const eligible = channels.filter(
    (c) => registry[c.type] && RANK[payload.severity] >= RANK[c.min_severity ?? 'low'],
  )
  const results = await Promise.allSettled(eligible.map((c) => registry[c.type].send(c.config, payload)))
  const delivered: string[] = []
  const failed: string[] = []
  results.forEach((r, i) => {
    if (r.status === 'fulfilled') delivered.push(eligible[i].type)
    else { failed.push(eligible[i].type); console.error(`canal ${eligible[i].type} falló:`, r.reason) }
  })
  return { delivered, failed }
}
```

Run: `pnpm vitest run src/lib/deliver` → Expected: PASS (3 tests) — los adapters aún no existen pero TypeScript los pide: crear los 5 archivos del Step 7 antes de correr, o correr solo `dispatch.test.ts` con stubs. Orden recomendado: Step 7 primero, luego correr.

- [ ] **Step 7: Canales base (webhook-family + email)** — todos son variaciones de POST con el texto compartido.

`src/lib/deliver/channels/slack.ts`:

```typescript
import type { ChannelAdapter } from '../types'
import { formatAlertText } from '../format'

export const slack: ChannelAdapter = {
  async send(config, payload) {
    const res = await fetch(config.webhook_url, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ text: formatAlertText(payload) }),
    })
    if (!res.ok) throw new Error(`slack ${res.status}`)
  },
}
```

`src/lib/deliver/channels/google-chat.ts` (mismo shape `{text}`):

```typescript
import type { ChannelAdapter } from '../types'
import { formatAlertText } from '../format'

export const googleChat: ChannelAdapter = {
  async send(config, payload) {
    const res = await fetch(config.webhook_url, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ text: formatAlertText(payload) }),
    })
    if (!res.ok) throw new Error(`google_chat ${res.status}`)
  },
}
```

`src/lib/deliver/channels/discord.ts` (Discord usa `content` y bold con `**`):

```typescript
import type { ChannelAdapter } from '../types'
import { formatAlertText } from '../format'

export const discord: ChannelAdapter = {
  async send(config, payload) {
    const res = await fetch(config.webhook_url, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ content: formatAlertText(payload).replaceAll('*', '**').replaceAll('_', '*') }),
    })
    if (!res.ok) throw new Error(`discord ${res.status}`)
  },
}
```

`src/lib/deliver/channels/teams.ts` (incoming webhook, `{text}` renderiza markdown):

```typescript
import type { ChannelAdapter } from '../types'
import { formatAlertText } from '../format'

export const teams: ChannelAdapter = {
  async send(config, payload) {
    const res = await fetch(config.webhook_url, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ text: formatAlertText(payload).replaceAll('*', '**') }),
    })
    if (!res.ok) throw new Error(`teams ${res.status}`)
  },
}
```

`src/lib/deliver/channels/email.ts` (Resend; para el hackathon el `from` de sandbox basta):

```typescript
import type { ChannelAdapter } from '../types'
import { formatAlertText } from '../format'

export const email: ChannelAdapter = {
  async send(config, payload) {
    const res = await fetch('https://api.resend.com/emails', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${process.env.RESEND_API_KEY}` },
      body: JSON.stringify({
        from: 'CumplIA <onboarding@resend.dev>',
        to: [config.address],
        subject: `⚠️ Cambio normativo: ${payload.norm_title.slice(0, 80)}`,
        text: formatAlertText(payload).replaceAll('*', '').replaceAll('_', ''),
      }),
    })
    if (!res.ok) throw new Error(`resend ${res.status}`)
  },
}
```

Run: `pnpm vitest run src/lib/deliver && pnpm build` → Expected: PASS + build limpio.

- [ ] **Step 8: Endpoint de matching** — `src/app/api/cron/match/route.ts` (nota: no importa ningún canal — solo el dispatcher):

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { supabaseAdmin } from '@/lib/supabase/admin'
import { matches } from '@/lib/match'
import { generateImpact } from '@/lib/deliver/impact'
import { deliverAlert } from '@/lib/deliver/dispatch'
import type { Company, Norm } from '@/lib/types'

export const maxDuration = 300

export async function POST(req: NextRequest) {
  if (req.headers.get('authorization') !== `Bearer ${process.env.CRON_SECRET}`)
    return NextResponse.json({ error: 'unauthorized' }, { status: 401 })

  const db = supabaseAdmin()
  const { data: companies } = await db.from('companies').select('*')
  const { data: norms } = await db.from('norms').select('*').not('analyzed_at', 'is', null)
  const { data: existing } = await db.from('alerts').select('company_id, norm_id')
  const seen = new Set((existing ?? []).map((a) => `${a.company_id}:${a.norm_id}`))

  let sent = 0
  const channelStats: Record<string, number> = {}
  for (const company of (companies ?? []) as Company[]) {
    for (const norm of (norms ?? []) as Norm[]) {
      if (seen.has(`${company.id}:${norm.id}`) || !matches(company, norm)) continue
      const { impact, recommendation } = await generateImpact(company, norm)
      await db.from('alerts').insert({ company_id: company.id, norm_id: norm.id, impact, recommendation })
      const { delivered } = await deliverAlert(company.channels ?? [], {
        norm_title: norm.title, norm_url: norm.url, impact, recommendation,
        severity: (norm.severity ?? 'low') as 'low' | 'medium' | 'high',
      })
      delivered.forEach((t) => { channelStats[t] = (channelStats[t] ?? 0) + 1 })
      sent++
    }
  }
  return NextResponse.json({ sent, channels: channelStats })
}
```

- [ ] **Step 9: Verificar end-to-end** — prerequisito: una empresa guardada (Task 4) con al menos 2 canales reales configurados (p. ej. webhook de Slack de un canal de prueba + email de un miembro).

Run: `curl -s -X POST -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/match`
Expected: `{"sent":N,"channels":{"slack":N,"email":N}}` con N≥1, mensaje visible en Slack Y correo recibido, filas nuevas en `alerts`. Romper a propósito un webhook (URL inválida) y re-disparar con alerts borradas → el otro canal sigue llegando (aislamiento de errores). Re-ejecutar el curl → `{"sent":0}` (idempotencia).

- [ ] **Step 10: Commit**

```bash
git add src/lib/match.ts src/lib/match.test.ts src/lib/deliver src/app/api/cron/match && git commit -m "feat: matching perfil-norma y entrega multicanal via adapters"
```

---

### Task 5b: Canales wow — WhatsApp (Kapso) + llamada de voz (Retell) (M2 tras Task 4, o quien vaya adelantado)

**Files:**
- Create: `src/lib/deliver/channels/whatsapp.ts`
- Create: `src/lib/deliver/channels/voice.ts`
- Modify: `src/lib/deliver/dispatch.ts` (2 líneas: registrar ambos en `ADAPTERS`)

**Interfaces:**
- Consumes: `ChannelAdapter`, `AlertPayload`, `formatAlertText` (Task 5)
- Produces: adapters `whatsapp` y `voice` registrados. Cero cambios en pipeline, matching o UI (los campos ya existen desde Task 4).
- **Prerequisitos manuales (hacer temprano — dependen de terceros):**
  1. Cuenta en [Kapso](https://kapso.ai) (es del ecosistema Platanus — mencionarlo en el pitch), conectar un número WhatsApp de prueba, copiar `KAPSO_API_KEY` y `KAPSO_PHONE_NUMBER_ID` (Project Settings → API Keys).
  2. Cuenta en [Retell AI](https://retellai.com), comprar/usar número de prueba, crear un agente con este prompt y copiar `RETELL_API_KEY`, `RETELL_FROM_NUMBER`, `RETELL_AGENT_ID`:

> Eres CumplIA, asistente de cumplimiento normativo. Llamas para avisar de una norma crítica. Di: "Hola, te llamo de CumplIA. Se publicó {{norm_title}}. {{impact}} Nuestra recomendación: {{recommendation}}. Te enviamos el detalle por escrito. ¿Quieres que te lo repita?". Sé breve y claro, español colombiano neutro. Si pide repetir, repite. Luego despídete.

- [ ] **Step 1: Adapter WhatsApp** — `src/lib/deliver/channels/whatsapp.ts`:

```typescript
import type { ChannelAdapter } from '../types'
import { formatAlertText } from '../format'

// Kapso proxya el WhatsApp Cloud API: https://docs.kapso.ai/api/meta/whatsapp/messages/send-a-message
export const whatsapp: ChannelAdapter = {
  async send(config, payload) {
    const res = await fetch(
      `https://api.kapso.ai/meta/whatsapp/v24.0/${process.env.KAPSO_PHONE_NUMBER_ID}/messages`,
      {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'X-API-Key': process.env.KAPSO_API_KEY! },
        body: JSON.stringify({
          messaging_product: 'whatsapp',
          to: config.phone,
          type: 'text',
          text: { body: formatAlertText(payload).replaceAll('_', '') },
        }),
      },
    )
    if (!res.ok) throw new Error(`kapso ${res.status}: ${await res.text()}`)
  },
}
```

- [ ] **Step 2: Adapter de voz** — `src/lib/deliver/channels/voice.ts`:

```typescript
import type { ChannelAdapter } from '../types'

// Retell crea la llamada saliente; las dynamic variables se inyectan al prompt del agente:
// https://docs.retellai.com/api-references/create-phone-call
export const voice: ChannelAdapter = {
  async send(config, payload) {
    const res = await fetch('https://api.retellai.com/v2/create-phone-call', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${process.env.RETELL_API_KEY}` },
      body: JSON.stringify({
        from_number: process.env.RETELL_FROM_NUMBER,
        to_number: config.phone,
        override_agent_id: process.env.RETELL_AGENT_ID,
        retell_llm_dynamic_variables: {
          norm_title: payload.norm_title,
          impact: payload.impact,
          recommendation: payload.recommendation,
        },
      }),
    })
    if (!res.ok) throw new Error(`retell ${res.status}: ${await res.text()}`)
  },
}
```

- [ ] **Step 3: Registrar en el dispatcher** — en `src/lib/deliver/dispatch.ts`, agregar los imports y dos entradas a `ADAPTERS`:

```typescript
import { whatsapp } from './channels/whatsapp'
import { voice } from './channels/voice'
// ...dentro de ADAPTERS:
  whatsapp,
  voice,
```

- [ ] **Step 4: Verificar end-to-end** — configurar en `/settings` el WhatsApp y el teléfono de voz del presentador; borrar las alerts de la empresa demo en Supabase para re-disparar.

Run: `curl -s -X POST -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/match`
Expected: llega el WhatsApp con el texto de la alerta Y suena el teléfono con la llamada de CumplIA leyendo la norma (solo para la norma `high` — seed-001/seed-002; verificar que una norma `medium` NO dispara llamada).

- [ ] **Step 5: Commit**

```bash
git add src/lib/deliver && git commit -m "feat: canales WhatsApp (Kapso) y llamada de voz (Retell)"
```

---

### Task 6: Feed de alertas en el dashboard (M3)

**Files:**
- Create: `src/app/(dashboard)/feed/page.tsx`

**Interfaces:**
- Consumes: `createServerSupabase()` (Task 3), filas de `alerts` (Task 5)
- Produces: página `/feed`. **M4 agrega aquí su botón "Generar PR" (Task 7 Step 4).**

- [ ] **Step 1: Página del feed** — `src/app/(dashboard)/feed/page.tsx`:

```tsx
import { createServerSupabase } from '@/lib/supabase/server'

export default async function Feed() {
  const supabase = await createServerSupabase()
  const { data: alerts } = await supabase
    .from('alerts')
    .select('*, norms(title, url, severity, published_at)')
    .order('created_at', { ascending: false })

  if (!alerts?.length) return <p>Sin alertas aún. Configura tu empresa en Configuración.</p>
  return (
    <div style={{ display: 'grid', gap: 16 }}>
      <h2>Alertas normativas</h2>
      {alerts.map((a) => (
        <article key={a.id} style={{ border: '1px solid #ddd', borderRadius: 8, padding: 16 }}>
          <strong>{a.norms.title}</strong> <small>({a.norms.severity})</small>
          <p><b>Cómo te afecta:</b> {a.impact}</p>
          <p><b>Qué hacer:</b> {a.recommendation}</p>
          {a.norms.url && <a href={a.norms.url}>Ver norma</a>}
          {a.pr_url && <p>✅ <a href={a.pr_url}>PR de cumplimiento abierto</a></p>}
        </article>
      ))}
    </div>
  )
}
```

- [ ] **Step 2: Verificar** — Run: `pnpm dev`, login, abrir `/feed`.
Expected: las alertas creadas en Task 5 listadas con impacto y recomendación (RLS garantiza que solo ve las de su empresa).

- [ ] **Step 3: Commit**

```bash
git add "src/app/(dashboard)/feed" && git commit -m "feat: feed de alertas normativas"
```

---

### Task 7: PRO — análisis de repo + PR + revisor asignado (M4)

**Files:**
- Create: `src/lib/pro/github.ts`
- Create: `src/app/api/pro/pr/route.ts`
- Create: `src/app/(dashboard)/feed/pr-button.tsx`
- Modify: `src/app/(dashboard)/feed/page.tsx` (agregar botón)

**Interfaces:**
- Consumes: `alerts`+`norms`+`companies` (Tasks 0/5), `anthropic`, `MODEL`; `GITHUB_TOKEN` (PAT clásico con scope `repo` de una cuenta bot con acceso al repo demo)
- Produces: `openCompliancePR(args: { repo: string; reviewer: string | null; normTitle: string; obligations: Obligation[]; impact: string }): Promise<string>` (retorna URL del PR); endpoint `POST /api/pro/pr` body `{ alertId: string }`.
- **Prerequisito manual:** crear un repo demo (p. ej. `equipo/facturador-demo`) con 3-4 archivos TypeScript realistas de facturación (un `invoice.ts` con generación de XML, un `logger.ts` con retención de 1 año) — el objetivo es que la norma seed-001/seed-002 tenga algo real que corregir.

- [ ] **Step 1: Cliente GitHub + generación del cambio** — `src/lib/pro/github.ts`:

```typescript
import { Octokit } from 'octokit'
import { anthropic, MODEL } from '@/lib/llm'
import type { Obligation } from '@/lib/types'

const octokit = () => new Octokit({ auth: process.env.GITHUB_TOKEN })

type FileChange = { path: string; content: string }

async function proposeChanges(
  files: { path: string; content: string }[],
  normTitle: string, obligations: Obligation[], impact: string,
): Promise<{ changes: FileChange[]; pr_body: string }> {
  const msg = await anthropic.messages.create({
    model: MODEL,
    max_tokens: 8000,
    system: `Eres un ingeniero que adapta código para cumplir una norma colombiana. Responde SOLO JSON:
{"changes":[{"path":"ruta/archivo.ts","content":"CONTENIDO COMPLETO del archivo modificado"}],
 "pr_body":"markdown: qué norma, qué obliga, qué cambiaste y por qué, qué debe verificar el revisor"}
Cambia SOLO lo necesario para la obligación. Si ningún archivo es relevante, changes=[].`,
    messages: [{ role: 'user', content:
      `NORMA: ${normTitle}\nOBLIGACIONES: ${JSON.stringify(obligations)}\nIMPACTO: ${impact}\n\nARCHIVOS DEL REPO:\n` +
      files.map((f) => `=== ${f.path} ===\n${f.content}`).join('\n\n') }],
  })
  const text = msg.content[0].type === 'text' ? msg.content[0].text : '{}'
  return JSON.parse(text.slice(text.indexOf('{'), text.lastIndexOf('}') + 1))
}

export async function openCompliancePR(args: {
  repo: string; reviewer: string | null; normTitle: string; obligations: Obligation[]; impact: string
}): Promise<string> {
  const [owner, repo] = args.repo.split('/')
  const gh = octokit()

  // 1. Leer archivos fuente del repo (código, no node_modules)
  const { data: tree } = await gh.rest.git.getTree({ owner, repo, tree_sha: 'HEAD', recursive: 'true' })
  const paths = (tree.tree ?? [])
    .filter((t) => t.type === 'blob' && /\.(ts|tsx|js|json|md)$/.test(t.path ?? '') && !t.path?.includes('node_modules'))
    .slice(0, 15).map((t) => t.path!) 
  const files = await Promise.all(paths.map(async (path) => {
    const { data } = await gh.rest.repos.getContent({ owner, repo, path })
    const content = 'content' in data ? Buffer.from(data.content, 'base64').toString() : ''
    return { path, content }
  }))

  // 2. Proponer cambios con Claude
  const { changes, pr_body } = await proposeChanges(files, args.normTitle, args.obligations, args.impact)
  if (!changes.length) throw new Error('el modelo no encontró archivos que corregir')

  // 3. Branch + commits + PR
  const { data: repoInfo } = await gh.rest.repos.get({ owner, repo })
  const base = repoInfo.default_branch
  const { data: baseRef } = await gh.rest.git.getRef({ owner, repo, ref: `heads/${base}` })
  const branch = `complia/cumplimiento-${Date.now()}`
  await gh.rest.git.createRef({ owner, repo, ref: `refs/heads/${branch}`, sha: baseRef.object.sha })
  for (const change of changes) {
    const { data: current } = await gh.rest.repos.getContent({ owner, repo, path: change.path, ref: branch })
    await gh.rest.repos.createOrUpdateFileContents({
      owner, repo, path: change.path, branch,
      message: `fix: cumplimiento — ${args.normTitle.slice(0, 60)}`,
      content: Buffer.from(change.content).toString('base64'),
      sha: 'sha' in current ? current.sha : undefined,
    })
  }
  const { data: pr } = await gh.rest.pulls.create({
    owner, repo, base, head: branch,
    title: `[CumplIA] Cumplimiento: ${args.normTitle.slice(0, 80)}`,
    body: `${pr_body}\n\n---\n🤖 PR generado por CumplIA. **Requiere revisión humana — nunca mergear sin aprobar.**`,
  })
  if (args.reviewer)
    await gh.rest.pulls.requestReviewers({ owner, repo, pull_number: pr.number, reviewers: [args.reviewer] })
      .catch((e) => console.error('no se pudo asignar reviewer:', e))
  return pr.html_url
}
```

- [ ] **Step 2: Endpoint** — `src/app/api/pro/pr/route.ts`:

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { supabaseAdmin } from '@/lib/supabase/admin'
import { openCompliancePR } from '@/lib/pro/github'

export const maxDuration = 300

export async function POST(req: NextRequest) {
  const { alertId } = await req.json()
  const db = supabaseAdmin()
  const { data: alert } = await db.from('alerts').select('*, norms(*), companies(*)').eq('id', alertId).single()
  if (!alert) return NextResponse.json({ error: 'alert no encontrada' }, { status: 404 })
  if (!alert.companies.github_repo) return NextResponse.json({ error: 'empresa sin repo configurado' }, { status: 400 })

  const prUrl = await openCompliancePR({
    repo: alert.companies.github_repo,
    reviewer: alert.companies.reviewer_github,
    normTitle: alert.norms.title,
    obligations: alert.norms.obligations,
    impact: alert.impact,
  })
  await db.from('alerts').update({ pr_url: prUrl }).eq('id', alertId)
  return NextResponse.json({ prUrl })
}
```

- [ ] **Step 3: Botón en el feed** — `src/app/(dashboard)/feed/pr-button.tsx`:

```tsx
'use client'
import { useState } from 'react'

export function PrButton({ alertId }: { alertId: string }) {
  const [state, setState] = useState<'idle' | 'working' | 'done' | 'error'>('idle')
  const [url, setUrl] = useState('')
  const go = async () => {
    setState('working')
    const res = await fetch('/api/pro/pr', { method: 'POST', body: JSON.stringify({ alertId }) })
    const json = await res.json()
    if (res.ok) { setUrl(json.prUrl); setState('done') } else setState('error')
  }
  if (state === 'done') return <a href={url}>✅ Ver PR de cumplimiento</a>
  return (
    <button onClick={go} disabled={state === 'working'}>
      {state === 'working' ? 'Analizando tu código…' : state === 'error' ? 'Falló — reintentar' : '⚙️ Generar PR de cumplimiento (PRO)'}
    </button>
  )
}
```

- [ ] **Step 4: Integrar en el feed** — en `src/app/(dashboard)/feed/page.tsx`, importar `import { PrButton } from './pr-button'` y dentro del `<article>`, después del link "Ver norma", reemplazar la línea `{a.pr_url && ...}` por:

```tsx
{a.pr_url ? <p>✅ <a href={a.pr_url}>PR de cumplimiento abierto</a></p> : <PrButton alertId={a.id} />}
```

- [ ] **Step 5: Verificar end-to-end** — con el repo demo creado, la empresa con `github_repo`/`reviewer_github` configurados y una alerta de seed-001:

Run: clic en "Generar PR de cumplimiento" en `/feed`.
Expected: PR abierto en el repo demo con diff coherente con la obligación, reviewer solicitado, body explicando la norma; `alerts.pr_url` poblado; el feed muestra el link. **Verificar que el PR quedó en estado abierto (no mergeado).**

- [ ] **Step 6: Commit**

```bash
git add src/lib/pro src/app/api/pro "src/app/(dashboard)/feed" && git commit -m "feat: tier PRO - análisis de repo, PR de cumplimiento y revisor asignado"
```

---

### Task 8: MCP server (HTTP + paquete npm) (M5)

**Files:**
- Create: `src/app/api/mcp/route.ts`
- Create: `src/app/api/public/norms/route.ts` (API pública read-only que consume el paquete npm)
- Create: `packages/mcp-npm/package.json`
- Create: `packages/mcp-npm/server.mjs`
- Create: `packages/mcp-npm/README.md`

**Interfaces:**
- Consumes: `supabaseAdmin()`, tabla `norms` (Task 0), `SECTORS`
- Produces: (a) endpoint MCP streamable-HTTP en `/api/mcp`; (b) `GET /api/public/norms?sector=&q=&limit=` (JSON read-only, solo normas ya analizadas); (c) paquete npm **`complia-mcp`** — MCP stdio instalable con `npx complia-mcp` que proxya la API pública. Ambas superficies exponen los mismos tools `buscar_normas` y `normas_por_sector`.
- **Decisión de seguridad:** el paquete npm NO lleva credenciales — solo hace fetch a la API pública (las normas son datos públicos). La service-role key jamás sale del servidor.
- Duplicación deliberada de tool-defs entre `/api/mcp` y el paquete (es un cliente delgado de ~60 líneas); unificar post-hackathon si se vuelve molesto.

- [ ] **Step 1: Handler MCP** — `src/app/api/mcp/route.ts`:

```typescript
import { createMcpHandler } from 'mcp-handler'
import { z } from 'zod'
import { supabaseAdmin } from '@/lib/supabase/admin'
import { SECTORS } from '@/lib/types'

const handler = createMcpHandler((server) => {
  server.tool(
    'buscar_normas',
    'Busca normativa colombiana reciente por texto libre en el título/resumen.',
    { query: z.string(), limit: z.number().max(20).default(5) },
    async ({ query, limit }) => {
      const db = supabaseAdmin()
      const { data } = await db.from('norms')
        .select('title, issuer, published_at, summary, sectors, severity, url')
        .or(`title.ilike.%${query}%,summary.ilike.%${query}%`)
        .not('analyzed_at', 'is', null)
        .order('published_at', { ascending: false }).limit(limit)
      return { content: [{ type: 'text', text: JSON.stringify(data ?? [], null, 2) }] }
    },
  )
  server.tool(
    'normas_por_sector',
    `Normativa reciente que afecta a un sector. Sectores válidos: ${SECTORS.join(', ')}.`,
    { sector: z.enum(SECTORS), limit: z.number().max(20).default(10) },
    async ({ sector, limit }) => {
      const db = supabaseAdmin()
      const { data } = await db.from('norms')
        .select('title, issuer, published_at, summary, obligations, severity, url')
        .contains('sectors', [sector])
        .order('published_at', { ascending: false }).limit(limit)
      return { content: [{ type: 'text', text: JSON.stringify(data ?? [], null, 2) }] }
    },
  )
})

export { handler as GET, handler as POST }
```

- [ ] **Step 2: Verificar con MCP Inspector**

Run: `pnpm dev` y en otra terminal `npx @modelcontextprotocol/inspector` → conectar a `http://localhost:3000/api/mcp` (transport: Streamable HTTP) → ejecutar `normas_por_sector` con `sector: "fintech"`.
Expected: JSON con las normas fintech del seed.

- [ ] **Step 3: API pública read-only** (la superficie que consume el paquete npm) — `src/app/api/public/norms/route.ts`:

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { supabaseAdmin } from '@/lib/supabase/admin'

export async function GET(req: NextRequest) {
  const { searchParams } = new URL(req.url)
  const sector = searchParams.get('sector')
  const q = searchParams.get('q')
  const limit = Math.min(Number(searchParams.get('limit') ?? 10), 20)

  const db = supabaseAdmin()
  let query = db.from('norms')
    .select('title, issuer, norm_type, published_at, summary, sectors, obligations, severity, url')
    .not('analyzed_at', 'is', null)
    .order('published_at', { ascending: false })
    .limit(limit)
  if (sector) query = query.contains('sectors', [sector])
  if (q) query = query.or(`title.ilike.%${q}%,summary.ilike.%${q}%`)

  const { data, error } = await query
  if (error) return NextResponse.json({ error: 'query failed' }, { status: 500 })
  return NextResponse.json(data)
}
```

Run: `curl -s "http://localhost:3000/api/public/norms?sector=fintech"`
Expected: JSON con las normas fintech del seed.

- [ ] **Step 4: Paquete npm** — `packages/mcp-npm/package.json` (verificar en npmjs.com que el nombre `complia-mcp` esté libre; fallback: scoped `@<org-del-equipo>/complia-mcp`):

```json
{
  "name": "complia-mcp",
  "version": "0.1.0",
  "description": "MCP server de CumplIA — normativa colombiana estructurada, consultable por agentes de IA",
  "type": "module",
  "bin": { "complia-mcp": "./server.mjs" },
  "files": ["server.mjs", "README.md"],
  "engines": { "node": ">=20" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.0"
  }
}
```

`packages/mcp-npm/server.mjs` (cliente stdio delgado — sin credenciales, solo API pública):

```javascript
#!/usr/bin/env node
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js'
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js'
import { z } from 'zod'

const API = process.env.COMPLIA_API_URL ?? 'https://complia.vercel.app' // ajustar a la URL real del deploy
const SECTORS = ['fintech', 'salud', 'alimentos', 'transporte', 'construccion',
  'comercio', 'tecnologia', 'datos-personales', 'laboral-general', 'tributario-general']

async function fetchNorms(params) {
  const qs = new URLSearchParams(Object.entries(params).filter(([, v]) => v != null && v !== ''))
  const res = await fetch(`${API}/api/public/norms?${qs}`)
  if (!res.ok) throw new Error(`CumplIA API ${res.status}`)
  return res.json()
}

const asText = (data) => ({ content: [{ type: 'text', text: JSON.stringify(data, null, 2) }] })

const server = new McpServer({ name: 'complia', version: '0.1.0' })

server.tool(
  'buscar_normas',
  'Busca normativa colombiana reciente por texto libre en título/resumen.',
  { query: z.string(), limit: z.number().max(20).default(5) },
  async ({ query, limit }) => asText(await fetchNorms({ q: query, limit })),
)

server.tool(
  'normas_por_sector',
  `Normativa colombiana reciente que afecta a un sector. Válidos: ${SECTORS.join(', ')}.`,
  { sector: z.enum(SECTORS), limit: z.number().max(20).default(10) },
  async ({ sector, limit }) => asText(await fetchNorms({ sector, limit })),
)

await server.connect(new StdioServerTransport())
```

`packages/mcp-npm/README.md`:

```markdown
# complia-mcp

Normativa colombiana estructurada para agentes de IA. Datos: SUIN-Juriscol + Diario Oficial, estructurados con IA por [CumplIA].

## Uso

Claude Code: `claude mcp add complia -- npx -y complia-mcp`

Claude Desktop (`claude_desktop_config.json`):
{ "mcpServers": { "complia": { "command": "npx", "args": ["-y", "complia-mcp"] } } }

Tools: `buscar_normas(query)` · `normas_por_sector(sector)`
```

- [ ] **Step 5: Probar el paquete en local (sin publicar)**

Run: `cd packages/mcp-npm && npm install && COMPLIA_API_URL=http://localhost:3000 npx @modelcontextprotocol/inspector node server.mjs` (con `pnpm dev` corriendo en otra terminal) → ejecutar `normas_por_sector` con `sector: "fintech"`.
Expected: mismas normas del seed que devuelve `/api/mcp`.

- [ ] **Step 6: Publicar a npm** (después del deploy de Task 9, con la URL real en la constante `API`):

```bash
cd packages/mcp-npm && npm login && npm publish --access public
npx -y complia-mcp # smoke test post-publicación (debe arrancar sin crash)
```

Expected: paquete visible en npmjs.com; `claude mcp add complia -- npx -y complia-mcp` funciona en una máquina limpia.

- [ ] **Step 7: Conectar a Claude Desktop para la demo** — vía el paquete publicado (`npx -y complia-mcp` en la config) o el connector HTTP (`https://<app>.vercel.app/api/mcp`), y probar el prompt: *"¿qué normativa fintech salió este mes en Colombia?"*.
Expected: Claude llama `normas_por_sector` y responde con las normas reales.

- [ ] **Step 8: Commit**

```bash
git add src/app/api/mcp src/app/api/public packages/mcp-npm && git commit -m "feat: MCP server HTTP + paquete npm complia-mcp"
```

---

### Task 9: Deploy + cron + checklist de demo (M5)

**Files:**
- Create: `vercel.json`
- Create: `docs/DEMO.md`

**Interfaces:**
- Consumes: todo lo anterior mergeado a `main`
- Produces: app en producción con cron diario y guion de demo ensayable.

- [ ] **Step 1: Cron de Vercel** — `vercel.json`:

```json
{
  "crons": [
    { "path": "/api/cron/ingest", "schedule": "0 11 * * *" },
    { "path": "/api/cron/match", "schedule": "30 11 * * *" }
  ]
}
```

Nota: los crons de Vercel llegan por GET sin body — agregar en ambos route handlers un `export const GET = POST` no sirve porque validan header; para el hackathon basta el disparo manual por curl y dejar el cron como evidencia de arquitectura. (Post-hackathon: validar `CRON_SECRET` vía query param en GET.)

- [ ] **Step 2: Deploy**

```bash
npx vercel --prod
```

Configurar en el dashboard de Vercel las 6 env vars de Global Constraints. Agregar la URL de producción a los redirect URLs permitidos en Supabase (Authentication → URL Configuration).
Expected: login con Google funciona en la URL de producción; `curl -X POST -H "Authorization: Bearer $CRON_SECRET" https://<app>.vercel.app/api/cron/match` responde 200.

- [ ] **Step 3: Checklist de demo** — `docs/DEMO.md`:

```markdown
# Demo CumplIA (3 min) — checklist pre-escenario

Estado previo (preparar 30 min antes, NO en vivo):
- [ ] Empresa demo configurada (fintech SAS) con canales: Slack #complia-demo, WhatsApp del presentador, voz al teléfono del presentador (volumen alto)
- [ ] Kapso y Retell con crédito/número activo verificado ese mismo día
- [ ] Repo facturador-demo restaurado (borrar PRs/branches complia/* de ensayos: `gh pr list -R equipo/facturador-demo`)
- [ ] Borrar alerts de la empresa demo en Supabase para re-disparar en vivo
- [ ] Claude Desktop conectado al MCP de producción
- [ ] Pestañas abiertas: PDF Diario Oficial de ayer, /settings, Slack, /feed, repo demo

Guion:
1. PDF del Diario Oficial: "¿alguien sabe si esto le aplica a su empresa? Salieron 4 ayer." (20s)
2. Cifras: 4 normas/día (+104%), 5.237 h/año, 45% no se entera. (40s)
3. curl al /api/cron/match en vivo → llega la alerta a Slack + WhatsApp, y SUENA EL TELÉFONO:
   contestar en altavoz, CumplIA lee la norma en voz alta. Momento wow. (40s)
4. /feed → clic "Generar PR de cumplimiento" → mostrar el PR con diff + reviewer asignado. (50s)
5. Claude Desktop: "¿qué normativa fintech salió este mes en Colombia?" → tool call en vivo. (30s)
6. Cierre: "La ley ya es machine-readable. Solo que nadie la había conectado." (10s)

Plan B si falla la red/LLM en vivo: video de respaldo grabado en el ensayo de la hora 30.
```

- [ ] **Step 4: Ensayo completo del guion contra producción** — correr los 6 pasos del guion de punta a punta, cronometrar (<3 min), grabar el video de respaldo.
Expected: todos los pasos pasan sin tocar código.

- [ ] **Step 5: Commit**

```bash
git add vercel.json docs/DEMO.md && git commit -m "chore: deploy config, cron y guion de demo"
```

---

## Self-review (hecho al escribir el plan)

- **Cobertura vs anteproyecto:** SSO ✅ (T3) · dashboard config con tipo/sector ✅ (T4) · canales designados multicanal (Slack/Google Chat/Discord/Teams/email/WhatsApp vía Kapso/voz vía Retell) con patrón adapter desacoplado ✅ (T4+T5+T5b) · Plus alertas qué-cambió/cómo-afecta/qué-hacer ✅ (T5) · PRO codebase→PR→reviewer que aprueba ✅ (T7, nunca mergea) · MCP/ACCESS ✅ (T8) · escalable a países ✅ (columna `country`, adaptador por fuente) · free tier/boletín y Diario Oficial scraping declarados fuera de alcance en Global Constraints.
- **Desacople de canales verificado:** agregar un canal = 1 archivo nuevo + 1 línea en `ADAPTERS` + 1 campo en settings; el pipeline (`/api/cron/match`) solo conoce `deliverAlert`. Un canal caído no bloquea a los demás (test de aislamiento en T5 Step 5). La política de ruido vive en config (`min_severity`), no en los adapters.
- **Riesgo señalado, no oculto:** los campos SODA de SUIN están verificados con curl real (T1 Step 1), y las URLs de SFC/SIC responden 200 server-rendered (T1c); los regex de los índices scrapeados (DIAN, SFC, SIC) pueden requerir ajuste contra el HTML vivo — cada task trae su paso de validación; si cualquier fuente falla, `ingestAll` la aísla (-1 en stats) y el seed sostiene la demo completa.
- **Simetría de arquitectura (argumento de pitch):** fuentes de entrada y canales de salida usan el mismo patrón — un contrato (`SourceAdapter`/`ChannelAdapter`), un archivo por implementación, un registro central, aislamiento de fallos. Agregar Superfinanciera o Telegram mañana es el mismo movimiento de 1 archivo + 1 línea.
- **Consistencia de tipos:** `matches`/`generateImpact`/`sendSlackAlert`/`openCompliancePR` se definen una vez y se consumen con la misma firma; columnas snake_case en DB = campos TS.
